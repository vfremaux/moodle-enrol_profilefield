This enrolment method provides an interactive
way to be enrolled in a course based on some 
custom (or not curstom) profile field value.

If profile field is not configured, runs just as
the manual enrol plugin.